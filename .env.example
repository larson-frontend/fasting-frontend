###############################################
# Frontend Environment Example Configuration  #
###############################################

# Base URL of the backend API (must include /api if backend exposes that prefix)
VITE_API_BASE=http://localhost:8080/api

# Explicitly enable mock data (DO NOT USE IN PRODUCTION)
# If omitted and running in dev mode, mocks are enabled automatically.
# In production builds mocks are always disabled regardless of this flag.
# VITE_USE_MOCK_DATA=true

# Optional comma separated list of feature flags to enable (e.g. detailedNotifications,themeSelection)
# VITE_ENABLE_FEATURES=detailedNotifications,themeSelection

# Log level: debug | info | warn | error (default: info in production, debug in dev)
# VITE_LOG_LEVEL=debug

# HTTP request timeout in milliseconds (default 8000)
# VITE_REQUEST_TIMEOUT_MS=8000

# Maximum automatic retries for idempotent GET requests (default 2)
# VITE_HTTP_MAX_RETRIES=2

# Backoff multiplier for retries (default 2)
# VITE_HTTP_RETRY_BACKOFF_FACTOR=2

# Initial backoff delay in ms (default 250)
# VITE_HTTP_RETRY_BACKOFF_MS=250

